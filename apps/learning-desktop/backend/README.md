# learning-desktop backend

`apps/learning-desktop` 的独立后端入口（复用 `@simpagent/runtime-node`）。

- `SIMPAGENT_PROJECT_ID=learning-desktop`
- `SIMPAGENT_DATA_DIR=./data`
- `PORT=3012`

启动：

```bash
npm run --workspace @simpagent/learning-backend dev
```

