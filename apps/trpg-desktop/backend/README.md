# trpg-desktop backend

`apps/trpg-desktop` 的独立后端入口（复用 `@simpagent/runtime-node`）。

- `SIMPAGENT_PROJECT_ID=trpg-desktop`
- `SIMPAGENT_DATA_DIR=./data`
- `PORT=3022`

启动：

```bash
npm run --workspace @simpagent/trpg-backend dev
```

